const s = 'JavaScript es un lenguaje magistral';

const ss = s.replace('a' , 'X'); //Reemplaza la primer 'a' que se encuentre por una 'X'

const sss = s.replaceAll('a' , 'X'); //Sustituye TODAS las 'a' del string s por una 'X'

console.log(ss);
console.log(sss);