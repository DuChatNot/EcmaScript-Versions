// Operador nullish ??

const n = 1;
const val = n ?? 5; //El operador nullish (??) pregunta si la variable tiene un valor nulo, en caso de que lo sea le asigna el valor puesto a continuacion del operador.
console.log(val);